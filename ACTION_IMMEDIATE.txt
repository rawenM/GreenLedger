╔═══════════════════════════════════════════════════════════════════════════╗
║                                                                            ║
║                    🎯 ACTION IMMÉDIATE - 3 ÉTAPES                         ║
║                                                                            ║
╚═══════════════════════════════════════════════════════════════════════════╝

PROBLÈMES ACTUELS:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
❌ Les informations de détection de fraude ne s'affichent pas
❌ Les boutons d'actions (Valider/Bloquer/Supprimer) manquent
❌ L'email d'inscription n'est pas envoyé

CAUSE:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Les colonnes fraud_score et fraud_checked n'existent pas dans la base de données

SOLUTION EN 3 ÉTAPES (5 MINUTES):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌─────────────────────────────────────────────────────────────────────────┐
│ ÉTAPE 1: CRÉER LES COLONNES DANS LA BASE DE DONNÉES (2 min)             │
└─────────────────────────────────────────────────────────────────────────┘

1. Ouvrez votre navigateur web

2. Allez sur: http://localhost/phpmyadmin

3. Dans le menu de gauche, cliquez sur "greenledger"

4. Cliquez sur l'onglet "SQL" en haut de la page

5. Copiez et collez ce code SQL:

   ┌─────────────────────────────────────────────────────────────────────┐
   │ USE greenledger;                                                     │
   │                                                                      │
   │ ALTER TABLE `user`                                                   │
   │ ADD COLUMN IF NOT EXISTS `fraud_score` DOUBLE DEFAULT 0.0,          │
   │ ADD COLUMN IF NOT EXISTS `fraud_checked` BOOLEAN DEFAULT FALSE;     │
   │                                                                      │
   │ CREATE INDEX IF NOT EXISTS idx_fraud_score ON `user`(fraud_score);  │
   │ CREATE INDEX IF NOT EXISTS idx_fraud_checked                        │
   │     ON `user`(fraud_checked);                                        │
   │                                                                      │
   │ CREATE TABLE IF NOT EXISTS `fraud_detection_results` (              │
   │     `id` BIGINT NOT NULL AUTO_INCREMENT,                            │
   │     `user_id` BIGINT NOT NULL,                                      │
   │     `risk_score` DOUBLE NOT NULL,                                   │
   │     `risk_level` VARCHAR(20) NOT NULL,                              │
   │     `is_fraudulent` BOOLEAN NOT NULL DEFAULT FALSE,                 │
   │     `recommendation` VARCHAR(255),                                  │
   │     `analysis_details` TEXT,                                        │
   │     `analyzed_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,         │
   │     PRIMARY KEY (`id`),                                             │
   │     KEY `fk_fraud_user` (`user_id`),                                │
   │     KEY `idx_risk_level` (`risk_level`)                             │
   │ ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;                            │
   │                                                                      │
   │ SELECT 'Installation terminée!' AS resultat;                        │
   └─────────────────────────────────────────────────────────────────────┘

6. Cliquez sur le bouton "Exécuter" (en bas à droite)

7. Vous devriez voir: "Installation terminée!"

┌─────────────────────────────────────────────────────────────────────────┐
│ ÉTAPE 2: RECOMPILER L'APPLICATION (2 min)                               │
└─────────────────────────────────────────────────────────────────────────┘

1. Ouvrez un terminal (CMD ou PowerShell)

2. Tapez cette commande:

   mvn clean compile

3. Attendez que la compilation se termine (vous verrez "BUILD SUCCESS")

┌─────────────────────────────────────────────────────────────────────────┐
│ ÉTAPE 3: RELANCER L'APPLICATION (1 min)                                 │
└─────────────────────────────────────────────────────────────────────────┘

1. Si l'application est déjà lancée, fermez-la (Ctrl+C dans le terminal)

2. Relancez avec:

   run.bat

3. Attendez que l'application démarre

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

VÉRIFICATION:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Ouvrez l'application

2. Connectez-vous en tant qu'admin

3. Allez dans "Gestion des Utilisateurs"

4. Vous devriez maintenant voir:

   ✅ Statistiques de fraude en haut (🔴 Fraudes / 🟢 Sûrs / 🟡 À Examiner)
   ✅ Colonne "Score Fraude" avec badges colorés
   ✅ Bouton [Détails] pour voir l'analyse
   ✅ 4 boutons d'actions: ✓ (Valider) ⛔ (Bloquer) 🗑 (Supprimer) ✏️ (Éditer)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

TEST COMPLET:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Pour tester que TOUT fonctionne, créez un utilisateur suspect:

1. Cliquez sur "➕ Nouvel utilisateur"

2. Remplissez avec:
   - Nom: Test
   - Prénom: Fake
   - Email: test@tempmail.com
   - Téléphone: 1111111111
   - Adresse: test
   - Mot de passe: Test123!

3. Cliquez sur "Créer"

4. RÉSULTAT ATTENDU:
   
   Dans le terminal:
   ┌─────────────────────────────────────────────────────────────────────┐
   │ [FraudDetection] Analyse de l'inscription...                        │
   │ Score de risque: 70.0/100                                           │
   │ Niveau: CRITIQUE                                                     │
   │ [FraudDetection] ALERTE: Compte bloqué automatiquement              │
   └─────────────────────────────────────────────────────────────────────┘

   Dans l'interface:
   ┌─────────────────────────────────────────────────────────────────────┐
   │ Utilisateur apparaît avec:                                           │
   │ - Score: 70/100 - Critique 🔴                                       │
   │ - Statut: BLOQUÉ                                                     │
   │ - Bouton [Détails] cliquable                                        │
   │ - 4 boutons d'actions visibles                                      │
   └─────────────────────────────────────────────────────────────────────┘

5. Cliquez sur [Détails] pour voir l'analyse complète

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CONCERNANT L'EMAIL D'INSCRIPTION:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Le code pour envoyer l'email est déjà en place. Si l'email n'est pas envoyé:

1. Vérifiez que Gmail API est configuré:
   - Fichier: src/main/resources/credentials.json existe
   - Dossier: tokens/ contient les tokens OAuth2

2. Vérifiez le fichier .env:
   GMAIL_API_ENABLED=true
   GMAIL_FROM_EMAIL=ibrahimimajid058@gmail.com

3. Dans le terminal, vous devriez voir:
   [UnifiedEmail] Utilisation de Gmail API pour les emails

Si vous voyez des erreurs d'email, ce n'est pas critique - l'inscription 
fonctionne quand même. L'email est un bonus.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

FICHIERS IMPORTANTS:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ Code Java: DÉJÀ FAIT
   - AdminUsersController.java (interface avec fraude + boutons)
   - UserDAOImpl.java (lecture/écriture des champs fraude)
   - FraudDetectionService.java (analyse IA)
   - UserServiceImpl.java (envoi emails)

❌ Base de données: À FAIRE MAINTENANT (Étape 1 ci-dessus)
   - Colonnes fraud_score et fraud_checked manquantes
   - Table fraud_detection_results manquante

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

COMMENCEZ MAINTENANT! 🚀

Étape 1: http://localhost/phpmyadmin → Exécutez le SQL ci-dessus
Étape 2: mvn clean compile
Étape 3: run.bat

TEMPS TOTAL: 5 MINUTES

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
