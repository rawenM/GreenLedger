╔═══════════════════════════════════════════════════════════════════════════╗
║                                                                            ║
║                    ⚡ COMMANDES RAPIDES - COPIER/COLLER                   ║
║                                                                            ║
╚═══════════════════════════════════════════════════════════════════════════╝

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1️⃣  OUVRIR PHPMYADMIN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Dans votre navigateur:

    http://localhost/phpmyadmin

Puis:
- Cliquez sur "greenledger" (menu gauche)
- Cliquez sur "SQL" (onglet en haut)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2️⃣  SCRIPT SQL À EXÉCUTER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Copiez-collez ce code dans phpMyAdmin:

┌───────────────────────────────────────────────────────────────────────────┐
│ USE greenledger;                                                           │
│                                                                            │
│ ALTER TABLE `user`                                                         │
│ ADD COLUMN IF NOT EXISTS `fraud_score` DOUBLE DEFAULT 0.0,                │
│ ADD COLUMN IF NOT EXISTS `fraud_checked` BOOLEAN DEFAULT FALSE;           │
│                                                                            │
│ CREATE INDEX IF NOT EXISTS idx_fraud_score ON `user`(fraud_score);        │
│ CREATE INDEX IF NOT EXISTS idx_fraud_checked ON `user`(fraud_checked);    │
│                                                                            │
│ CREATE TABLE IF NOT EXISTS `fraud_detection_results` (                    │
│     `id` BIGINT NOT NULL AUTO_INCREMENT,                                  │
│     `user_id` BIGINT NOT NULL,                                            │
│     `risk_score` DOUBLE NOT NULL,                                         │
│     `risk_level` VARCHAR(20) NOT NULL,                                    │
│     `is_fraudulent` BOOLEAN NOT NULL DEFAULT FALSE,                       │
│     `recommendation` VARCHAR(255),                                        │
│     `analysis_details` TEXT,                                              │
│     `analyzed_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,               │
│     PRIMARY KEY (`id`),                                                   │
│     KEY `fk_fraud_user` (`user_id`),                                      │
│     KEY `idx_risk_level` (`risk_level`)                                   │
│ ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;                                  │
│                                                                            │
│ SELECT 'Installation terminée!' AS resultat;                              │
└───────────────────────────────────────────────────────────────────────────┘

Puis cliquez sur "Exécuter"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3️⃣  RECOMPILER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Dans le terminal (CMD ou PowerShell):

    mvn clean compile

Attendez: "BUILD SUCCESS"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4️⃣  RELANCER L'APPLICATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Si l'application est lancée, fermez-la d'abord:

    Ctrl+C

Puis relancez:

    run.bat

Attendez les messages:
    [FraudDetection] Colonne fraud_score détectée
    [FraudDetection] Colonne fraud_checked détectée
    Application started successfully

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5️⃣  VÉRIFIER LA BASE DE DONNÉES (OPTIONNEL)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Dans phpMyAdmin, exécutez:

┌───────────────────────────────────────────────────────────────────────────┐
│ SELECT COLUMN_NAME, COLUMN_TYPE                                           │
│ FROM INFORMATION_SCHEMA.COLUMNS                                           │
│ WHERE TABLE_SCHEMA = 'greenledger'                                        │
│   AND TABLE_NAME = 'user'                                                 │
│   AND COLUMN_NAME IN ('fraud_score', 'fraud_checked');                    │
└───────────────────────────────────────────────────────────────────────────┘

Résultat attendu:
┌─────────────────┬─────────────┐
│ COLUMN_NAME     │ COLUMN_TYPE │
├─────────────────┼─────────────┤
│ fraud_score     │ double      │
│ fraud_checked   │ tinyint(1)  │
└─────────────────┴─────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6️⃣  TESTER AVEC UN UTILISATEUR SUSPECT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Dans l'application:
1. Connectez-vous en tant qu'admin
2. Allez dans "Gestion des Utilisateurs"
3. Cliquez sur "➕ Nouvel utilisateur"
4. Remplissez:

    Nom:       Test
    Prénom:    Fake
    Email:     test@tempmail.com
    Téléphone: 1111111111
    Adresse:   test
    Mot de passe: Test123!

5. Cliquez sur "Créer"

Résultat attendu:
- Score: 70/100 - Critique 🔴
- Statut: BLOQUÉ
- Bouton [Détails] visible
- 4 boutons d'actions: ✓ ⛔ 🗑 ✏️

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 COMMANDES DE DIAGNOSTIC
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Voir tous les utilisateurs avec leurs scores:

┌───────────────────────────────────────────────────────────────────────────┐
│ SELECT id, nom, prenom, email, fraud_score, fraud_checked, statut        │
│ FROM `user`                                                               │
│ ORDER BY fraud_score DESC;                                                │
└───────────────────────────────────────────────────────────────────────────┘

Statistiques de fraude:

┌───────────────────────────────────────────────────────────────────────────┐
│ SELECT                                                                    │
│     COUNT(*) as total,                                                    │
│     SUM(CASE WHEN fraud_score >= 75 THEN 1 ELSE 0 END) as critique,      │
│     SUM(CASE WHEN fraud_score >= 50 AND fraud_score < 75                 │
│         THEN 1 ELSE 0 END) as moyen,                                      │
│     SUM(CASE WHEN fraud_score < 50 THEN 1 ELSE 0 END) as faible          │
│ FROM `user`                                                               │
│ WHERE fraud_checked = 1;                                                  │
└───────────────────────────────────────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔧 COMMANDES DE DÉPANNAGE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Nettoyer et recompiler complètement:

    mvn clean install -DskipTests

Vérifier la version de Java:

    java -version

Vérifier Maven:

    mvn -version

Voir les logs en temps réel:

    tail -f logs/application.log

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📁 FICHIERS IMPORTANTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Scripts SQL:
    fix-fraude-simple.sql          # Script principal à exécuter
    verifier-bdd.sql               # Vérification de la base
    database_fraud_detection.sql   # Script complet (alternatif)

Documentation:
    ACTION_IMMEDIATE.txt           # Guide rapide (ce fichier)
    A_FAIRE_MAINTENANT.md          # Guide complet
    AFFICHER_TOUT_MAINTENANT.txt   # Guide détaillé

Code Java:
    src/main/java/Controllers/AdminUsersController.java
    src/main/java/Services/FraudDetectionService.java
    src/main/java/dao/UserDAOImpl.java

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ CHECKLIST COMPLÈTE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

□ Ouvrir phpMyAdmin (http://localhost/phpmyadmin)
□ Sélectionner la base "greenledger"
□ Exécuter le script SQL ci-dessus
□ Voir "Installation terminée!"
□ Ouvrir un terminal
□ Exécuter: mvn clean compile
□ Voir "BUILD SUCCESS"
□ Fermer l'application si lancée (Ctrl+C)
□ Exécuter: run.bat
□ Voir les messages de détection des colonnes
□ Ouvrir l'application
□ Se connecter en admin
□ Aller dans "Gestion des Utilisateurs"
□ Voir les statistiques de fraude
□ Voir la colonne "Score Fraude"
□ Voir les 4 boutons d'actions
□ Créer un utilisateur suspect pour tester
□ Voir le score 70/100 - Critique 🔴
□ Cliquer sur [Détails] pour voir l'analyse
□ Tester les boutons d'actions

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

TEMPS TOTAL: 5 MINUTES ⏱️

COMMENCEZ MAINTENANT! 🚀

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
